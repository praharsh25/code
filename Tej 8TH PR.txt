CREATE TABLE EMPLOYEE10 (
EMP_ID NUMBER PRIMARY KEY,
EMP_NAME VARCHAR2(50),
SALARY NUMBER
);
INSERT INTO EMPLOYEE10 VALUES (1, 'Amit', 30000);
INSERT INTO EMPLOYEE10 VALUES (2, 'Neha', 40000);
COMMIT;
CREATE OR REPLACE PROCEDURE INCREMENT_SALARY(
E_ID IN NUMBER,
INC_AMT IN NUMBER
)
IS
BEGIN
UPDATE EMPLOYEE10
SET SALARY = SALARY + INC_AMT
WHERE EMP_ID = E_ID;

DBMS_OUTPUT.PUT_LINE('Salary Updated Successfully');
END;
/
SET SERVEROUTPUT ON;

BEGIN
    INCREMENT_SALARY(1, 5000);
END;
/

DECLARE
    RESULT NUMBER;
BEGIN
    RESULT := ANNUAL_SAL(1);
    DBMS_OUTPUT.PUT_LINE('Annual Salary = ' || RESULT);
END;
/

SELECT * FROM EMPLOYEE10;